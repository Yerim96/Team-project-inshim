<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="index.css" type="text/css">
    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>


    <title>Document</title>
</head>

<body>

    <header>
        <nav class="sub-header">
            <ul class="sub-menu">
                <li><a href="">InShim소개</a></li>
                <li><a href="">고객지원</a></li>
                <li><a href="">예약조회</a></li>
            </ul>
            <div class="sub-login">
                <a href="">로그인/회원가입</a>
            </div>
        </nav>
    </header>
    <header>
        <nav class="main-header">
            <div class="header-logo">
                <a href="#">INSHIM</a>
            </div>
            <ul class="main-menu">
                <li><a href="">지역소개</a></li>
                <li><a href="/userinfo" style="color: #B9F3E4;">코스보기</a></li>
                <li><a href="">후기보기</a></li>
                <li><a href="">프로모션</a></li>
            </ul>
        </nav>
    </header>
    <hr>
    <main>
        <div class='main-box'>
            <div class="main-box-1">
                <p>여행가고싶은지역</p>
                <form name="submit">
                    <select name="city" id="city">
                        <option value="all">모두</option>
                        <% for (let i=0; i < routeCity.length; i++ ){%>
                            <option value="<%= routeCity[i]%>">
                                <%= routeCity[i]%>
                            </option>
                            <%}%>
                    </select>
            </div>
            <div class="main-box-1">
                <p>기간</p>
                <select name="day" id="day">
                    <option value="all">모두</option>
                    <% for (let i=0; i < routeDay.length; i++ ){%>
                        <option value="<%= routeDay[i]%>">
                            <%= routeDay[i]%>
                        </option>
                        <%}%>
                </select>
                </form>
            </div>
        </div>
        <button class="button" type="button" onclick="submitValue()">검색</button>
    </main>
    <hr>
    <section>
        <div class="section-result">
            <div>sibal</div>
            <div>ahaha</div>
            <div>aaa</div>
        </div>
    </section>
    <div>
        <%=detailTable[0]%>
            <%=routeCity%>
    </div>
    <script>
        function submitValue() {
            const form = document.forms["submit"];
            axios({
                method: 'POST',
                url: '/userinfo',
                data: {
                    city: form.city.value,
                    day: form.day.value
                }
            }).then((res) => {
                // console.log('res.data', res.data.city)
                // console.log('detail', detailTable[0].detail_comment)
                document.querySelector('.section-result').innerHTML = res.data.detailTable;
            })
        }


    </script>
</body>

</html>